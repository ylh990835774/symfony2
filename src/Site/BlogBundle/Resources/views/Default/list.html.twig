<!DOCTYPE html>
<html>
<head>
	<title>LIST Article</title>
</head>
<body>
	<ul>
	{% for article in articles %}
	<li><a href="{{ path('site_blog_show',{'id': article.id}) }}" title="">{{ article.title }}</a></li>
	{% endfor %}
	</ul>
</body>
</html>